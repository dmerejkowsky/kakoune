var test_file_name = kak.get_option("string", "test_file_name")
try {
   use(test_file_name)
   kak.command("quit")
} catch (e) {
   kak.command("edit err")
   var buffer = kak.buffer()
   buffer.write(e)
   kak.command("write")
   kak.command("quit 2")
}
